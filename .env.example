# Groq API key — used by /api/coach (production) and scripts/ralph-loop.js (local).
# Get one (free, no card required) at https://console.groq.com → API Keys.
# NEVER commit your real key. This file is the template; actual key goes in .env (gitignored).
GROQ_API_KEY=gsk_your_key_here
